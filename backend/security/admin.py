from django.contrib import admin


from django.contrib import admin
from .models import SecurityPersonnel

# Register the SecurityPersonnel model with the Django admin interface
admin.site.register(SecurityPersonnel)
